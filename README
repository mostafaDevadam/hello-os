

create build folder

install packages

sudo apt install make asm qemu-system-x86_64 

# In Terminal

# build
make

# run
qemu-system-x86_64 -fda build/main_floppy.img

# from branch: asm-shell

It's support German and English keyboard

# remove old image
rm -rf build/*.bin build/*.img

# build
make


# run
qemu-system-x86_64 -fda build/shell_floppy.img

after updated asm-shell

# run
make clean && make

qemu-system-x86_64 -fda build/os_disk.img
